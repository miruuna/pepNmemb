var __index = {"config":{"lang":["en"],"separator":"[\\s\\-,:!=\\[\\]()\"`/]+|\\.(?!\\d)|&[lg]t;|(?!\\b)(?=[A-Z][a-z])","pipeline":["stopWordFilter"]},"docs":[{"location":"index.html","title":"pepNmemb","text":"<p>Welcome to the documentation for the <code>pepNmemb</code> code! </p>"},{"location":"pepNmemb_docs/about/changelog.html","title":"Changelog","text":""},{"location":"pepNmemb_docs/about/changelog.html#001","title":"[0.0.1]","text":""},{"location":"pepNmemb_docs/about/changelog.html#added","title":"Added","text":"<ul> <li>The initial release</li> </ul>"},{"location":"pepNmemb_docs/about/license.html","title":"License","text":"LICENSE.md"},{"location":"pepNmemb_docs/intro/resources.html","title":"Resources","text":""},{"location":"pepNmemb_docs/intro/why.html","title":"Why?","text":"<p>Analyse trajectories of MD simulations that include peptides in membranes.</p>"},{"location":"reference/SUMMARY.html","title":"SUMMARY","text":"<ul> <li>pepNmemb<ul> <li>scripts<ul> <li>classes</li> <li>get_hbonds_pep_lipid</li> <li>get_insertion</li> <li>get_interactions_inter_pep</li> <li>get_lipid_apl</li> <li>get_tilt_angle</li> <li>utils</li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/pepNmemb/scripts/classes.html","title":"classes","text":""},{"location":"reference/pepNmemb/scripts/classes.html#pepNmemb.scripts.classes.Peptide","title":"Peptide","text":"<pre><code>Peptide(pep_name: str, xtc_file_path: str, tpr_file_path: str, peptide_number: int, amino_acid_count: int, step_size: int)\n</code></pre> <p>Main Peptide Class for molecular dynamics analysis.</p> <p>Manages trajectory loading, peptide selection, and frame processing.</p> <p>Args:     pep_name (str): Name of the peptide.     xtc_file_path (str): Path to the XTC trajectory file.     tpr_file_path (str): Path to the TPR topology file.     peptide_number (int): Number of peptides.     amino_acid_count (int): Number of amino acids per peptide.     step_size (int): Step size for trajectory analysis.</p> <p>Raises:     ValueError: If input arguments are invalid.     RuntimeError: If there's an error loading the MDAnalysis Universe.</p> Source code in <code>pepNmemb/scripts/classes.py</code> <pre><code>def __init__(\n    self,\n    pep_name: str,\n    xtc_file_path: str,\n    tpr_file_path: str,\n    peptide_number: int,\n    amino_acid_count: int,\n    step_size: int,\n) -&gt; None:\n    \"\"\"\n    Initialize a Peptide object for molecular dynamics analysis.\n\n    Args:\n        pep_name (str): Name of the peptide.\n        xtc_file_path (str): Path to the XTC trajectory file.\n        tpr_file_path (str): Path to the TPR topology file.\n        peptide_number (int): Number of peptides.\n        amino_acid_count (int): Number of amino acids per peptide.\n        step_size (int): Step size for trajectory analysis.\n\n    Raises:\n        ValueError: If input arguments are invalid.\n        RuntimeError: If there's an error loading the MDAnalysis Universe.\n    \"\"\"\n    self._validate_inputs(\n        pep_name, xtc_file_path, tpr_file_path, peptide_number, amino_acid_count, step_size\n    )\n\n    self.xtc_file_path = xtc_file_path\n    self.tpr_file_path = tpr_file_path\n    self.peptide_number = peptide_number\n    self.amino_acid_count = amino_acid_count\n    self.pep_name = pep_name\n    self.step_size = step_size\n\n    self._initialize_universe()\n    self._process_protein_data()\n    self._create_peptide_dictionary()\n</code></pre>"},{"location":"reference/pepNmemb/scripts/classes.html#pepNmemb.scripts.classes.Peptide.amino_acid_count","title":"amino_acid_count  <code>instance-attribute</code>","text":"<pre><code>amino_acid_count = amino_acid_count\n</code></pre>"},{"location":"reference/pepNmemb/scripts/classes.html#pepNmemb.scripts.classes.Peptide.pep_name","title":"pep_name  <code>instance-attribute</code>","text":"<pre><code>pep_name = pep_name\n</code></pre>"},{"location":"reference/pepNmemb/scripts/classes.html#pepNmemb.scripts.classes.Peptide.peptide_number","title":"peptide_number  <code>instance-attribute</code>","text":"<pre><code>peptide_number = peptide_number\n</code></pre>"},{"location":"reference/pepNmemb/scripts/classes.html#pepNmemb.scripts.classes.Peptide.step_size","title":"step_size  <code>instance-attribute</code>","text":"<pre><code>step_size = step_size\n</code></pre>"},{"location":"reference/pepNmemb/scripts/classes.html#pepNmemb.scripts.classes.Peptide.tpr_file_path","title":"tpr_file_path  <code>instance-attribute</code>","text":"<pre><code>tpr_file_path = tpr_file_path\n</code></pre>"},{"location":"reference/pepNmemb/scripts/classes.html#pepNmemb.scripts.classes.Peptide.xtc_file_path","title":"xtc_file_path  <code>instance-attribute</code>","text":"<pre><code>xtc_file_path = xtc_file_path\n</code></pre>"},{"location":"reference/pepNmemb/scripts/classes.html#pepNmemb.scripts.classes.Peptide.load_traj","title":"load_traj","text":"<pre><code>load_traj() -&gt; Tuple[ndarray, int]\n</code></pre> <p>Load the trajectory and determine frame indices.</p> <p>Returns:     Tuple[np.ndarray, int]:     - frames: Array of selected frame indices     - n_frames: Number of frames</p> <p>Raises:     RuntimeError: If there's an error loading trajectory frames.</p> Source code in <code>pepNmemb/scripts/classes.py</code> <pre><code>def load_traj(self) -&gt; Tuple[np.ndarray, int]:\n    \"\"\"\n    Load the trajectory and determine frame indices.\n\n    Returns:\n        Tuple[np.ndarray, int]:\n        - frames: Array of selected frame indices\n        - n_frames: Number of frames\n\n    Raises:\n        RuntimeError: If there's an error loading trajectory frames.\n    \"\"\"\n    try:\n        start, stop_sim, _ = self.u.trajectory.check_slice_indices(None, None, None)\n        frames = np.arange(start, stop_sim, self.step_size)\n        n_frames = frames.size\n\n        return frames, n_frames\n    except Exception as e:\n        raise RuntimeError(f\"Error loading trajectory frames: {e}\")\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_hbonds_pep_lipid.html","title":"get_hbonds_pep_lipid","text":""},{"location":"reference/pepNmemb/scripts/get_hbonds_pep_lipid.html#pepNmemb.scripts.get_hbonds_pep_lipid.hbonds_per_res","title":"hbonds_per_res","text":"<pre><code>hbonds_per_res(obj: Peptide, step_size: int = 500) -&gt; DataFrame\n</code></pre> <p>Analyze hydrogen bonds for peptide residues in membrane.</p> <p>Args:     obj (Peptide): Peptide object containing trajectory and properties.     step_size (int, optional): Trajectory analysis step size. Defaults to 500.</p> <p>Returns:     pd.DataFrame: DataFrame with hydrogen bond information.</p> Source code in <code>pepNmemb/scripts/get_hbonds_pep_lipid.py</code> <pre><code>def hbonds_per_res(obj: Peptide, step_size: int = 500) -&gt; pd.DataFrame:\n    \"\"\"\n    Analyze hydrogen bonds for peptide residues in membrane.\n\n    Args:\n        obj (Peptide): Peptide object containing trajectory and properties.\n        step_size (int, optional): Trajectory analysis step size. Defaults to 500.\n\n    Returns:\n        pd.DataFrame: DataFrame with hydrogen bond information.\n    \"\"\"\n    hbonds: List[Tuple[str, int, float, float]] = []\n\n    u = obj.u\n    peptide_name = obj.pep_name\n    pep_num_dict = obj.pep_dict\n\n    for pep, residues in tqdm(pep_num_dict.items()):\n        res_count = 1\n\n        for res_id in tqdm(range(residues[0], residues[1] + 1)):\n            h_bonds = HBA(\n                u,\n                update_selections=True,\n                between=[\"resname POPE or resname POPG\", f\"resid {res_id}\"],\n            )\n\n            # Guess hydrogen and acceptor selections\n            protein_hydrogens_sel = h_bonds.guess_hydrogens(f\"resid {res_id}\")\n            protein_acceptors_sel = h_bonds.guess_acceptors(f\"resid {res_id}\")\n            membrane_hydrogens_sel = h_bonds.guess_hydrogens(\"resname POPE or resname POPG\")\n            membrane_acceptors_sel = h_bonds.guess_acceptors(\"resname POPE or resname POPG\")\n\n            # Update hydrogen and acceptor selections\n            h_bonds.hydrogens_sel = f\"({protein_hydrogens_sel}) or ({membrane_hydrogens_sel})\"\n            h_bonds.acceptors_sel = f\"({protein_acceptors_sel}) or ({membrane_acceptors_sel})\"\n\n            # Run hydrogen bond analysis\n            h_bonds.run(step=step_size)\n\n            # Collect hydrogen bond count and time data\n            hydrogen_count = h_bonds.count_by_time()\n            time_count = h_bonds.times\n            for i in range(len(time_count)):\n                hbonds.append((pep, res_count, hydrogen_count[i], time_count[i]))\n            res_count += 1\n\n    # Create DataFrame and process time data\n    df = pd.DataFrame(hbonds)\n    df.columns = [\"Peptide\", \"Resid\", \"Hbonds\", \"Time\"]\n    df[\"Time\"] = (df[\"Time\"].astype(float) / 1000).astype(int)\n    df = df.rename(columns={\"Time\": \"Time (ns)\"})\n\n    # Save to CSV\n    df.to_csv(f\"hbonds_{peptide_name}.csv\", index=False)\n    return df\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_hbonds_pep_lipid.html#pepNmemb.scripts.get_hbonds_pep_lipid.main","title":"main","text":"<pre><code>main() -&gt; None\n</code></pre> Source code in <code>pepNmemb/scripts/get_hbonds_pep_lipid.py</code> <pre><code>def main() -&gt; None:\n\n    parser = argparse.ArgumentParser(\n        description=\"Run hydrogen bond analysis for peptide in membrane\"\n    )\n\n    parser.add_argument(\"-xtc\", \"--xtc\", type=str, help=\"Input xtc file path\")\n    parser.add_argument(\"-tpr\", \"--tpr\", type=str, help=\"Input tpr file path\")\n    parser.add_argument(\"-pname\", \"--pep_name\", type=str, help=\"Peptide name used for saving data\")\n    parser.add_argument(\"-pnum\", \"--pep_num\", type=int, help=\"Number of peptides\")\n    parser.add_argument(\"-res\", \"--res_num\", type=int, help=\"Number of residues in each peptide\")\n    parser.add_argument(\"-ss\", \"--step_size\", type=int, default=500, help=\"Step size\")\n\n    args = parser.parse_args()\n\n    peptide = Peptide(\n        args.pep_name,\n        args.xtc,\n        args.tpr,\n        peptide_number=args.pep_num,\n        amino_acid_count=args.res_num,\n        step_size=args.step_size,\n    )\n\n    print(\n        f\"Starting hydrogen bond analysis for peptide {args.pep_name} \"\n        f\"found at {args.xtc}, {args.tpr}\"\n    )\n\n    hbonds_per_res(peptide, step_size=args.step_size)\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_insertion.html","title":"get_insertion","text":""},{"location":"reference/pepNmemb/scripts/get_insertion.html#pepNmemb.scripts.get_insertion.get_closest_lipid_z","title":"get_closest_lipid_z","text":"<pre><code>get_closest_lipid_z(res_pos: ndarray, lipids: ndarray, p_up: float, p_low: float) -&gt; Optional[float]\n</code></pre> <p>Finds the closest lipid in the Z direction to the given residue position.</p> <p>Parameters: - res_pos: Residue position - lipids: Array of lipid indexes - p_up: Mean z-position of P atoms in the upper bilayer - p_low: Mean z-position of P atoms in the lower bilayer</p> Source code in <code>pepNmemb/scripts/get_insertion.py</code> <pre><code>def get_closest_lipid_z(\n    res_pos: np.ndarray, lipids: np.ndarray, p_up: float, p_low: float\n) -&gt; Optional[float]:\n    \"\"\"\n    Finds the closest lipid in the Z direction to the given residue position.\n\n    Parameters:\n    - res_pos: Residue position\n    - lipids: Array of lipid indexes\n    - p_up: Mean z-position of P atoms in the upper bilayer\n    - p_low: Mean z-position of P atoms in the lower bilayer\n\n    \"\"\"\n\n    all_up = lipids\n    kdtree = KDTree(lipids)\n\n    for radius in [(float(p_up) - float(p_low)) / 2, (float(p_up) - float(p_low)), 20, 40, 80, 120]:\n\n        x = kdtree.query_ball_point(res_pos, radius, return_sorted=True)\n\n        if len(x[0]) == 0:\n            continue\n\n        closest_lipid_index, _ = get_index_shortest_distance(res_pos, all_up[x[0]])\n        return closest_lipid_index[2]\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_insertion.html#pepNmemb.scripts.get_insertion.get_index_shortest_distance","title":"get_index_shortest_distance","text":"<pre><code>get_index_shortest_distance(point_p: ndarray, b: ndarray) -&gt; Tuple[ndarray, float]\n</code></pre> <p>Finds the index of the shortest distance between a point and a set of points.</p> <p>Parameters: - point_p: Find the shortest distance to this point - b: Set of points</p> Source code in <code>pepNmemb/scripts/get_insertion.py</code> <pre><code>def get_index_shortest_distance(point_p: np.ndarray, b: np.ndarray) -&gt; Tuple[np.ndarray, float]:\n    \"\"\"\n    Finds the index of the shortest distance between a point and a set of points.\n\n    Parameters:\n    - point_p: Find the shortest distance to this point\n    - b: Set of points\n\n    \"\"\"\n\n    point_p = point_p[0]\n    distances_array = np.empty(len(b))\n\n    for i in range(len(b)):\n        p = b[i]\n        distances_array[i] = np.sqrt(\n            (point_p[1] - p[1]) ** 2 + (point_p[0] - p[0]) ** 2 + (point_p[2] - p[2]) ** 2\n        )\n    distances_array = distances_array[distances_array != 0]\n\n    return (b[np.argmin(distances_array)], np.min(distances_array))\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_insertion.html#pepNmemb.scripts.get_insertion.get_peptide_insertion","title":"get_peptide_insertion","text":"<pre><code>get_peptide_insertion(peptide_name: str, aminoacid_count: int, peptide_number: int, membrane_lipids: int, step_size: int, xtc_file_path: str, tpr_file_path: str, output_folder: str) -&gt; DataFrame\n</code></pre> <p>Analyzes peptide insertion into the membrane and saves results as a CSV file.</p> <p>Parameters: - peptide_name: Name of the peptide - aminoacid_count: Number of amino acids per peptide - peptide_number: Number of peptides - membrane_lipids: Number of lipids in the membrane - step_size: Step size for trajectory analysis - xtc_file_path: Path to the XTC trajectory file - tpr_file_path: Path to the TPR topology file</p> Source code in <code>pepNmemb/scripts/get_insertion.py</code> <pre><code>def get_peptide_insertion(\n    peptide_name: str,\n    aminoacid_count: int,\n    peptide_number: int,\n    membrane_lipids: int,\n    step_size: int,\n    xtc_file_path: str,\n    tpr_file_path: str,\n    output_folder: str,\n) -&gt; pd.DataFrame:\n    \"\"\"\n    Analyzes peptide insertion into the membrane and saves results as a CSV file.\n\n    Parameters:\n    - peptide_name: Name of the peptide\n    - aminoacid_count: Number of amino acids per peptide\n    - peptide_number: Number of peptides\n    - membrane_lipids: Number of lipids in the membrane\n    - step_size: Step size for trajectory analysis\n    - xtc_file_path: Path to the XTC trajectory file\n    - tpr_file_path: Path to the TPR topology file\n    \"\"\"\n\n    peptide = Peptide(\n        pep_name=peptide_name,\n        xtc_file_path=xtc_file_path,\n        tpr_file_path=tpr_file_path,\n        peptide_number=peptide_number,\n        amino_acid_count=aminoacid_count,\n        step_size=step_size,\n    )\n    u = peptide.u\n    z_pos_list = []\n    frames, n_frames = peptide.load_traj()\n    for _, _ in tqdm(enumerate(u.trajectory[frames]), total=n_frames):\n        u.select_atoms(\"resname POPG and name P\").positions\n        p_memb = u.select_atoms(\"resname POPG and name P\").positions\n        p_up = np.mean(p_memb[: int(membrane_lipids / 2)])\n        p_low = np.mean(p_memb[int(membrane_lipids / 2) :])\n\n        for pep, res_id_range in peptide.pep_dict.items():\n            res_count = 1\n            for res_id in range(res_id_range[0], res_id_range[1] + 1):\n                # Calculate the z pos of each residue one at a time\n                res_z_pos = np.mean(\n                    u.select_atoms(f\"name CA and resid {res_id}\").positions[:, [2]].astype(float)\n                )\n                res_pos = u.select_atoms(f\"name CA and resid {res_id}\").positions\n\n                # Determine the actual insertion of each residue\n                pbc_crossed = (\n                    1 if res_z_pos &gt; 0 and res_z_pos &lt; u.dimensions[2] / 2 else 0\n                )  # This assumes that peptides don't insert more than half than the membrane height\n\n                if pbc_crossed == 0:\n                    lipid_z = get_closest_lipid_z(\n                        res_pos, p_memb[: int(membrane_lipids / 2)], p_up, p_low\n                    )\n\n                    if lipid_z:\n                        pep_insertion = res_z_pos - lipid_z\n\n                        z_pos_list.append(\n                            (\n                                u.trajectory.time,\n                                pep,\n                                res_id,\n                                res_count,\n                                pep_insertion,\n                                p_up,\n                                p_low,\n                                lipid_z,\n                                res_z_pos,\n                                pbc_crossed,\n                            )\n                        )\n                        res_count += 1\n\n    df = pd.DataFrame(\n        z_pos_list,\n        columns=[\n            \"Time (ns)\",\n            \"Peptide_num\",\n            \"Resid\",\n            \"Residue\",\n            \"CA Z position\",\n            \"P_up\",\n            \"P low\",\n            \"lipid_z\",\n            \"res_z_pos\",\n            \"Crossed\",\n        ],\n    )\n\n    df[\"Residue\"] = df[\"Residue\"].astype(\"str\")\n    df[\"Time (ns)\"] = df[\"Time (ns)\"].astype(float) / 1000\n    df[\"Time (ns)\"] = df[\"Time (ns)\"].astype(int)\n    df.to_csv(f\"{output_folder}/insertion_{peptide.pep_name}_insertion_curv.csv\")\n\n    return df\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_insertion.html#pepNmemb.scripts.get_insertion.main","title":"main","text":"<pre><code>main()\n</code></pre> Source code in <code>pepNmemb/scripts/get_insertion.py</code> <pre><code>def main():\n    args_dict = {}\n\n    for k in [\n        \"peptide_name\",\n        \"aminoacid_count\",\n        \"peptide_number\",\n        \"membrane_lipids\",\n        \"step_size\",\n        \"xtc_file_path\",\n        \"tpr_file_path\",\n    ]:\n        args_dict[k] = None\n\n    parser = argparse.ArgumentParser(\n        description=\"Process a file with key-value pairs or command line arguments.\"\n    )\n\n    # Add file argument (optional)\n    parser.add_argument(\"-f\", \"--file\", help=\"Path to input file with key=value pairs\")\n\n    parser.add_argument(\"-xtc\", \"--xtc\", type=str, help=\"Input xtc file path\")\n    parser.add_argument(\"-tpr\", \"--tpr\", type=str, help=\"Input xtpr file path\")\n    parser.add_argument(\"-pname\", \"--pep_name\", type=str, help=\"Peptide name used for saving data\")\n    parser.add_argument(\"-pnum\", \"--pep_num\", type=int, help=\"Number of peptides\")\n    parser.add_argument(\"-res\", \"--res_num\", type=int, help=\"Number of residues in each peptide\")\n    parser.add_argument(\"-mlipids\", \"--memb_lipids\", type=int, help=\"Number of membrane lipids\")\n    parser.add_argument(\"-ss\", \"--step_size\", type=int, help=\"Step size\")\n    parser.add_argument(\"-o\", \"--output_folder\", type=int, help=\"Output folder\")\n\n    args = parser.parse_args()\n\n    if args.file:\n        file_dict = process_file(args.file)\n        args_dict.update(file_dict)\n        args_dict[\"step_size\"] = int(args_dict[\"step_size\"])\n        args_dict[\"aminoacid_count\"] = int(args_dict[\"aminoacid_count\"])\n        args_dict[\"membrane_lipids\"] = int(args_dict[\"membrane_lipids\"])\n        args_dict[\"peptide_number\"] = int(args_dict[\"peptide_number\"])\n    else:\n        args_dict[\"peptide_name\"] = args.pep_name\n        args_dict[\"xtc_file_path\"] = args.xtc\n        args_dict[\"tpr_file_path\"] = args.tpr\n        args_dict[\"peptide_number\"] = args.pep_num\n        args_dict[\"aminoacid_count\"] = args.res_num\n        args_dict[\"membrane_lipids\"] = args.memb_lipids\n        args_dict[\"step_size\"] = args.step_size\n        args_dict[\"output_folder\"] = args.output_folder\n    print(args_dict)\n    try:\n        validate_dict_values(args_dict)\n        print(\"All values validated successfully.\")\n    except NoneValueError as e:\n        print(f\"Error: {e}\")\n        sys.exit(1)\n\n    print(\n        f\"Starting analysis for peptide {args_dict['peptide_name']} \\\n            found at {args_dict['xtc_file_path']}, {args_dict['tpr_file_path']}\"\n    )\n    get_peptide_insertion(\n        args_dict[\"peptide_name\"],\n        args_dict[\"aminoacid_count\"],\n        args_dict[\"peptide_number\"],\n        args_dict[\"membrane_lipids\"],\n        args_dict[\"step_size\"],\n        args_dict[\"xtc_file_path\"],\n        args_dict[\"tpr_file_path\"],\n        args_dict[\"output_folder\"],\n    )\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_interactions_inter_pep.html","title":"get_interactions_inter_pep","text":""},{"location":"reference/pepNmemb/scripts/get_interactions_inter_pep.html#pepNmemb.scripts.get_interactions_inter_pep.get_interactions","title":"get_interactions","text":"<pre><code>get_interactions(obj)\n</code></pre> Source code in <code>pepNmemb/scripts/get_interactions_inter_pep.py</code> <pre><code>def get_interactions(obj):\n    u = obj.u\n    obj_name = obj.pep_name\n    pep_num = obj.peptide_number\n\n    frames, n_frames = obj.load_traj()\n\n    all_list = []\n    for _, _ in tqdm(enumerate(u.trajectory[frames]), total=n_frames):\n        pair_done = []\n        for pep in range(1, pep_num):\n            res_range = obj.pep_dict[pep]\n            for pep2 in range(pep + 1, pep_num + 1):\n                res_range2 = obj.pep_dict[pep2]\n                for res in range(res_range[0], res_range[1] + 1):\n                    for res2 in range(res_range2[0], res_range2[1] + 1):\n                        selection1 = u.select_atoms(\"resid %s\" % res).center_of_mass()\n                        selection2 = u.select_atoms(\"resid %s\" % res2).center_of_mass()\n                        dist = distance_array(\n                            selection1, selection2, box=u.dimensions, result=None, backend=\"serial\"\n                        )\n                        min_dist = dist\n                        all_list.append(\n                            (\n                                res,\n                                res2,\n                                pep,\n                                pep2,\n                                min_dist[0].astype(float)[0],\n                                int(u.trajectory.time / 1000),\n                            )\n                        )\n            pair_done.append((pep, pep2))\n\n    df = pd.DataFrame(\n        all_list, columns=[\"Res1\", \"Res2\", \"Peptide1\", \"Peptide2\", \"mindist\", \"Time(ns)\"]\n    )\n    df.to_csv(f\"pepNmemb/data/interactions_{obj_name}.csv\")\n    return df\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_interactions_inter_pep.html#pepNmemb.scripts.get_interactions_inter_pep.main","title":"main","text":"<pre><code>main()\n</code></pre> Source code in <code>pepNmemb/scripts/get_interactions_inter_pep.py</code> <pre><code>def main():\n    parser = argparse.ArgumentParser(description=\"Run insertion into the membranr analysis\")\n\n    parser.add_argument(\"-xtc\", \"--xtc\", type=str, help=\"Input xtc file path\")\n    parser.add_argument(\"-tpr\", \"--tpr\", type=str, help=\"Input xtpr file path\")\n    parser.add_argument(\"-pname\", \"--pep_name\", type=str, help=\"Peptide name used for saving data\")\n    parser.add_argument(\"-pnum\", \"--pep_num\", type=int, help=\"Number of peptides\")\n    parser.add_argument(\"-res\", \"--res_num\", type=int, help=\"Number of residues in each peptide\")\n\n    parser.add_argument(\"-ss\", \"--step_size\", type=int, help=\"Step size\")\n\n    args = parser.parse_args()\n\n    peptide_name = args.pep_name\n    xtc_file_path = args.xtc\n    tpr_file_path = args.tpr\n    peptide_number = args.pep_num\n    aminoacid_count = args.res_num\n    step_size = args.step_size\n\n    print(f\"Starting analysis for peptide {peptide_name} found at {xtc_file_path}, {tpr_file_path}\")\n\n    peptide = Peptide(\n        peptide_name,\n        xtc_file_path,\n        tpr_file_path,\n        peptide_number=peptide_number,\n        amino_acid_count=aminoacid_count,\n        step_size=step_size,\n    )\n\n    get_interactions(peptide)\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_lipid_apl.html","title":"get_lipid_apl","text":"<p>Compute Lipid APL using Voronoi Tesselation</p>"},{"location":"reference/pepNmemb/scripts/get_lipid_apl.html#pepNmemb.scripts.get_lipid_apl.calc_and_write_to_file","title":"calc_and_write_to_file","text":"<pre><code>calc_and_write_to_file(path: str, membrane_type: str, results_directory: str) -&gt; None\n</code></pre> <p>Calculate area per lipid and write results to a CSV file.</p> <p>Args:     path (str): Path to molecular dynamics simulation files.     membrane_type (str): Type of membrane.     results_directory (str): Directory to save results.</p> <p>Returns:     None</p> Source code in <code>pepNmemb/scripts/get_lipid_apl.py</code> <pre><code>def calc_and_write_to_file(path: str, membrane_type: str, results_directory: str) -&gt; None:\n    \"\"\"\n    Calculate area per lipid and write results to a CSV file.\n\n    Args:\n        path (str): Path to molecular dynamics simulation files.\n        membrane_type (str): Type of membrane.\n        results_directory (str): Directory to save results.\n\n    Returns:\n        None\n    \"\"\"\n    Path(results_directory).mkdir(parents=True, exist_ok=True)\n    all_lipid: Dict[str, pd.DataFrame] = {}\n\n    if os.path.isdir(path):\n        peptide_name = os.path.basename(path)\n        print(f\"Starting calculations for single peptide -- {peptide_name}\")\n        df_lipid = calculate_apl(path)\n        df_lipid.to_csv(f\"{results_directory}/apl_lipid_{peptide_name}_{membrane_type}.csv\")\n        print(f\"{peptide_name} --- DONE\")\n        all_lipid[peptide_name] = df_lipid\n    else:\n        print(\"Invalid path provided\")\n\n    return\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_lipid_apl.html#pepNmemb.scripts.get_lipid_apl.calculate_apl","title":"calculate_apl","text":"<pre><code>calculate_apl(path: str) -&gt; DataFrame\n</code></pre> <p>Calculate area per lipid for a given molecular dynamics trajectory.</p> <p>Args:     path (str): Path to molecular dynamics simulation files.</p> <p>Returns:     pandas.DataFrame: DataFrame with area per lipid for each frame.</p> Source code in <code>pepNmemb/scripts/get_lipid_apl.py</code> <pre><code>def calculate_apl(path: str) -&gt; pd.DataFrame:\n    \"\"\"\n    Calculate area per lipid for a given molecular dynamics trajectory.\n\n    Args:\n        path (str): Path to molecular dynamics simulation files.\n\n    Returns:\n        pandas.DataFrame: DataFrame with area per lipid for each frame.\n    \"\"\"\n    start, stop, step = 15000, None, 500\n\n    apl_per_res_dict: Dict[str, np.ndarray] = {}\n\n    all_lipid_sel = \"(resname POPG and name C2 C21 C31) or (resname POPE and name C2 C21 C31)\"\n    all_protein_and_lipid = (\n        \"(resname POPG and name C2 C21 C31) or protein or \" \"(resname POPE and name C2 C21 C31)\"\n    )\n\n    # Load universe\n    u = mda.Universe(f\"{path}/md_0_1.tpr\", f\"{path}/md_0_1_combined_first500ns_pbc.xtc\")\n\n    # Determine unique lipid species and Voronoi seeds\n    membrane = u.select_atoms(all_lipid_sel).residues\n    all_select = u.select_atoms(all_protein_and_lipid).residues\n    unique_resnames = np.unique(membrane.resnames)\n    num_residues = {lipid: sum(membrane.resnames == lipid) for lipid in unique_resnames}\n    num_seeds = {\n        lipid: int(\n            u.select_atoms(f\"({all_lipid_sel}) and resname {lipid}\").n_atoms / num_residues[lipid]\n        )\n        for lipid in unique_resnames\n    }\n\n    # Output array initialization\n    all_apl = np.full(\n        (len([res.resid for res in all_select]), int(np.ceil(u.trajectory.n_frames / float(step)))),\n        fill_value=np.nan,\n        dtype=np.float32,\n    )\n\n    for ts in u.trajectory[start:stop:step]:\n        # Atoms must be within the unit cell\n        membrane.atoms.wrap(inplace=True)\n        box = ts.dimensions\n\n        frame_apl = np.asarray([res.resid for res in all_select], dtype=np.float32)\n\n        midpoint = np.mean(membrane.atoms.select_atoms(\"name P\").positions[:, 2])\n\n        # Calculate area per lipid for lower and upper leaflets\n        for leaflet_sign in [\"&lt;\", \"&gt;\"]:\n            # Freud.order.Voronoi requires z positions set to 0\n            leaflet = membrane.atoms.select_atoms(\n                f\"({all_lipid_sel}) and prop z {leaflet_sign} {midpoint}\"\n            ).residues\n            atoms = leaflet.atoms.select_atoms(all_lipid_sel)\n            pos = atoms.positions\n            pos[:, 2] = 0\n\n            # Check whether any atoms are overlapping in xy-plane\n            pos = remove_overlapping(pos)\n\n            # Voronoi tessellation to get area per cell\n            areas = get_all_areas(pos, box)\n\n            # Calculate area per lipid\n            frame_apl = get_area_per_lipid(\n                unique_resnames=unique_resnames,\n                atom_group=atoms,\n                all_areas=areas,\n                num_seeds=num_seeds,\n                area_array=frame_apl,\n            )\n\n        # Store data for this frame\n        all_apl[:, ts.frame // step] = frame_apl\n\n    for res in unique_resnames:\n        apl_per_res_dict[res] = all_apl[u.select_atoms(f\"resname {res}\").residues.resindices]\n\n    lipid_data = all_apl[u.select_atoms(\"resname POPG\").residues.resindices]\n    df_lipid = pd.DataFrame.from_records(lipid_data)\n    df_lipid.index = range(1, 513)\n    df_lipid.columns = range(1, np.size(df_lipid, 1) + 1)\n\n    return df_lipid\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_lipid_apl.html#pepNmemb.scripts.get_lipid_apl.get_all_areas","title":"get_all_areas","text":"<pre><code>get_all_areas(positions: ndarray, box: Union[ndarray, Tuple[float, float]]) -&gt; ndarray\n</code></pre> <p>Perform Voronoi tessellation and return area of each Voronoi cell.</p> <p>Args:     positions (numpy.ndarray): XY coordinates of atomic positions.     box (numpy.ndarray or tuple): Dimensions of the system (x, y).</p> <p>Returns:     numpy.ndarray: Areas of Voronoi cells.</p> Source code in <code>pepNmemb/scripts/get_lipid_apl.py</code> <pre><code>def get_all_areas(positions: np.ndarray, box: Union[np.ndarray, Tuple[float, float]]) -&gt; np.ndarray:\n    \"\"\"\n    Perform Voronoi tessellation and return area of each Voronoi cell.\n\n    Args:\n        positions (numpy.ndarray): XY coordinates of atomic positions.\n        box (numpy.ndarray or tuple): Dimensions of the system (x, y).\n\n    Returns:\n        numpy.ndarray: Areas of Voronoi cells.\n    \"\"\"\n    voro = freud.locality.Voronoi()\n    areas = voro.compute(\n        system={\"Lx\": box[0], \"Ly\": box[1], \"dimensions\": 2}, positions=positions\n    ).volumes\n\n    return areas\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_lipid_apl.html#pepNmemb.scripts.get_lipid_apl.get_area_per_lipid","title":"get_area_per_lipid","text":"<pre><code>get_area_per_lipid(unique_resnames: List[str], atom_group: AtomGroup, all_areas: ndarray, num_seeds: Dict[str, int], area_array: ndarray) -&gt; ndarray\n</code></pre> <p>Calculate area per lipid given Voronoi cell areas.</p> <p>Args:     unique_resnames (List[str]): Lipid species in the membrane.     atom_group (MDAnalysis.core.groups.AtomGroup): Atoms for Voronoi tessellation.     all_areas (numpy.ndarray): Areas of each cell in tessellation.     num_seeds (Dict[str, int]): Number of Voronoi seeds per lipid species.     area_array (numpy.ndarray): Array to store area per lipid.</p> <p>Returns:     numpy.ndarray: Modified area_array with area per lipid.</p> Source code in <code>pepNmemb/scripts/get_lipid_apl.py</code> <pre><code>def get_area_per_lipid(\n    unique_resnames: List[str],\n    atom_group: mda.core.groups.AtomGroup,\n    all_areas: np.ndarray,\n    num_seeds: Dict[str, int],\n    area_array: np.ndarray,\n) -&gt; np.ndarray:\n    \"\"\"\n    Calculate area per lipid given Voronoi cell areas.\n\n    Args:\n        unique_resnames (List[str]): Lipid species in the membrane.\n        atom_group (MDAnalysis.core.groups.AtomGroup): Atoms for Voronoi tessellation.\n        all_areas (numpy.ndarray): Areas of each cell in tessellation.\n        num_seeds (Dict[str, int]): Number of Voronoi seeds per lipid species.\n        area_array (numpy.ndarray): Array to store area per lipid.\n\n    Returns:\n        numpy.ndarray: Modified area_array with area per lipid.\n    \"\"\"\n    for res in unique_resnames:\n        lipid_indices = np.where(atom_group.resnames == res)\n        lipid_apl = all_areas[lipid_indices]\n\n        # Sum area contribution of each cell for a given lipid\n        lipid_apl = np.sum(\n            lipid_apl.reshape(atom_group[lipid_indices].residues.n_residues, num_seeds[res]), axis=1\n        )\n\n        # Store area per lipid for current lipid species\n        lipid_resindices = atom_group.select_atoms(f\"resname {res}\").residues.resindices\n        area_array[lipid_resindices] = lipid_apl\n\n    return area_array\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_lipid_apl.html#pepNmemb.scripts.get_lipid_apl.main","title":"main","text":"<pre><code>main() -&gt; None\n</code></pre> Source code in <code>pepNmemb/scripts/get_lipid_apl.py</code> <pre><code>def main() -&gt; None:\n    b_dir = \"path\"\n    output_dir = \"APL\"\n    Path(output_dir).mkdir(parents=True, exist_ok=True)\n\n    p = b_dir  # Replace with actual path if needed\n    calc_and_write_to_file(p, \"pg\", output_dir)\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_lipid_apl.html#pepNmemb.scripts.get_lipid_apl.remove_overlapping","title":"remove_overlapping","text":"<pre><code>remove_overlapping(positions: ndarray) -&gt; ndarray\n</code></pre> <p>Adjust xy positions if any pair of xy coordinates are identical.</p> <p>Given an Nx3 array of atomic positions, make minor adjustments to xy positions if any pair of xy coordinates are identical. If atoms are overlapping in xy, Freud will complain when attempting to perform the Voronoi tessellation.</p> <p>Args:     positions (numpy.ndarray): Array of atomic positions with shape (N, 3).</p> <p>Returns:     numpy.ndarray: Adjusted positions to remove xy overlaps.</p> Source code in <code>pepNmemb/scripts/get_lipid_apl.py</code> <pre><code>def remove_overlapping(positions: np.ndarray) -&gt; np.ndarray:\n    \"\"\"\n    Adjust xy positions if any pair of xy coordinates are identical.\n\n    Given an Nx3 array of atomic positions, make minor adjustments to xy\n    positions if any pair of xy coordinates are identical. If atoms are\n    overlapping in xy, Freud will complain when attempting to perform\n    the Voronoi tessellation.\n\n    Args:\n        positions (numpy.ndarray): Array of atomic positions with shape (N, 3).\n\n    Returns:\n        numpy.ndarray: Adjusted positions to remove xy overlaps.\n    \"\"\"\n    # Check whether any atoms are overlapping in the xy-plane\n    unique, indices, counts = np.unique(positions, return_index=True, return_counts=True, axis=0)\n\n    # If so, add a small distance between the two atoms (1e-3 A)\n    # in the x-dimension\n    if max(counts &gt; 1):\n        for duplicate_index in indices[counts &gt; 1]:\n            positions[duplicate_index, 0] += 0.001\n\n    return positions\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_tilt_angle.html","title":"get_tilt_angle","text":"<p>Get angle of insertion of peptides relative to the membrane plane</p>"},{"location":"reference/pepNmemb/scripts/get_tilt_angle.html#pepNmemb.scripts.get_tilt_angle.angle_between_vectors","title":"angle_between_vectors","text":"<pre><code>angle_between_vectors(v1: Tuple[float, float, float], v2: Tuple[float, float, float]) -&gt; float\n</code></pre> <p>Calculate the angle between two vectors in degrees.</p> <p>Args:     v1 (Tuple[float, float, float]): First vector.     v2 (Tuple[float, float, float]): Second vector.</p> <p>Returns:     float: Angle between vectors in degrees.</p> Source code in <code>pepNmemb/scripts/get_tilt_angle.py</code> <pre><code>def angle_between_vectors(v1: Tuple[float, float, float], v2: Tuple[float, float, float]) -&gt; float:\n    \"\"\"\n    Calculate the angle between two vectors in degrees.\n\n    Args:\n        v1 (Tuple[float, float, float]): First vector.\n        v2 (Tuple[float, float, float]): Second vector.\n\n    Returns:\n        float: Angle between vectors in degrees.\n    \"\"\"\n    return m.acos(dot(v1, v2) / (vector_length(v1) * vector_length(v2))) * (180 / m.pi)\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_tilt_angle.html#pepNmemb.scripts.get_tilt_angle.dot","title":"dot","text":"<pre><code>dot(v1: Tuple[float, float, float], v2: Tuple[float, float, float]) -&gt; float\n</code></pre> <p>Calculate the dot product of two vectors.</p> <p>Args:     v1 (Tuple[float, float, float]): First vector.     v2 (Tuple[float, float, float]): Second vector.</p> <p>Returns:     float: Dot product of the two vectors.</p> Source code in <code>pepNmemb/scripts/get_tilt_angle.py</code> <pre><code>def dot(v1: Tuple[float, float, float], v2: Tuple[float, float, float]) -&gt; float:\n    \"\"\"\n    Calculate the dot product of two vectors.\n\n    Args:\n        v1 (Tuple[float, float, float]): First vector.\n        v2 (Tuple[float, float, float]): Second vector.\n\n    Returns:\n        float: Dot product of the two vectors.\n    \"\"\"\n    return sum([c1 * c2 for c1, c2 in zip(v1, v2)])\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_tilt_angle.html#pepNmemb.scripts.get_tilt_angle.find_consecutive_sublists","title":"find_consecutive_sublists","text":"<pre><code>find_consecutive_sublists(integers: List[int]) -&gt; List[List[int]]\n</code></pre> <p>Find all consecutive sublists within a list.</p> <p>Args:     integers (List[int]): Input list of integers to find consecutive sublists.</p> <p>Returns:     List[List[int]]: List of consecutive sublists with at least 5 elements.</p> Source code in <code>pepNmemb/scripts/get_tilt_angle.py</code> <pre><code>def find_consecutive_sublists(integers: List[int]) -&gt; List[List[int]]:\n    \"\"\"\n    Find all consecutive sublists within a list.\n\n    Args:\n        integers (List[int]): Input list of integers to find consecutive sublists.\n\n    Returns:\n        List[List[int]]: List of consecutive sublists with at least 5 elements.\n    \"\"\"\n    if not integers:\n        return []\n\n    # Sort the list\n    integers.sort()\n\n    # Initialize variables\n    result: List[List[int]] = []\n    current_sublist = [integers[0]]\n\n    # Iterate through the sorted list\n    for i in range(1, len(integers)):\n        # Check if the current integer is consecutive to the previous one\n        if integers[i] == integers[i - 1] + 1:\n            current_sublist.append(integers[i])\n        else:\n            # If the current sublist has at least 5 consecutive integers, add it to the result\n            if len(current_sublist) &gt;= 5:\n                result.append(current_sublist)\n            # Start a new sublist\n            current_sublist = [integers[i]]\n\n    # Check the last sublist\n    if len(current_sublist) &gt;= 5:\n        result.append(current_sublist)\n\n    return result\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_tilt_angle.html#pepNmemb.scripts.get_tilt_angle.get_coords_spanning_memb","title":"get_coords_spanning_memb","text":"<pre><code>get_coords_spanning_memb(u: Universe, residues: Tuple[int, int], p_up: float) -&gt; Tuple[List[List[List[float]]], List[int]]\n</code></pre> <p>Compute coordinates of peptide residues spanning the membrane.</p> <p>Selects residues within -6 to +10 of the average Z-position of P atoms in the upper membrane layer.</p> <p>Args:     u (mda.Universe): MDAnalysis Universe object.     residues (Tuple[int, int]): Range of residue IDs.     p_up (float): Average Z-position of upper membrane layer.</p> <p>Returns:     Tuple[List[List[List[float]]], List[int]]:     - List of coordinate lists for selected residues     - List of selected residue IDs</p> Source code in <code>pepNmemb/scripts/get_tilt_angle.py</code> <pre><code>def get_coords_spanning_memb(\n    u: mda.Universe, residues: Tuple[int, int], p_up: float\n) -&gt; Tuple[List[List[List[float]]], List[int]]:\n    \"\"\"\n    Compute coordinates of peptide residues spanning the membrane.\n\n    Selects residues within -6 to +10 of the average Z-position\n    of P atoms in the upper membrane layer.\n\n    Args:\n        u (mda.Universe): MDAnalysis Universe object.\n        residues (Tuple[int, int]): Range of residue IDs.\n        p_up (float): Average Z-position of upper membrane layer.\n\n    Returns:\n        Tuple[List[List[List[float]]], List[int]]:\n        - List of coordinate lists for selected residues\n        - List of selected residue IDs\n    \"\"\"\n    pep_coords: List[List[List[float]]] = []\n    selected_res: List[int] = []\n\n    for res_id in range(residues[0], residues[1] + 1):\n        peptide_atoms = u.select_atoms(f\"name CA and resid {res_id}\").positions\n\n        z_pep = peptide_atoms[:, [2]].astype(float)\n        # inside the radius\n        if z_pep &lt; p_up + 10 and z_pep &gt; p_up - 6:\n            selected_res.append(res_id + 1)\n\n    final_residues = find_consecutive_sublists(selected_res)\n    pep_coords = [\n        [\n            u.select_atoms(f\"name CA and resid {res_id}\")\n            .positions.reshape(-1)\n            .astype(float)\n            .tolist()\n            for res_id in sub_list\n            if len(\n                u.select_atoms(f\"name CA and resid {res_id}\")\n                .positions.reshape(-1)\n                .astype(float)\n                .tolist()\n            )\n            &gt; 0\n        ]\n        for sub_list in final_residues\n    ]\n\n    return pep_coords, selected_res\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_tilt_angle.html#pepNmemb.scripts.get_tilt_angle.get_vector","title":"get_vector","text":"<pre><code>get_vector(coord1: Tuple[float, float, float], coord2: Tuple[float, float, float]) -&gt; Tuple[float, float, float]\n</code></pre> <p>Calculate the vector between two coordinates.</p> <p>Args:     coord1 (Tuple[float, float, float]): First coordinate.     coord2 (Tuple[float, float, float]): Second coordinate.</p> <p>Returns:     Tuple[float, float, float]: Vector from coord2 to coord1.</p> Source code in <code>pepNmemb/scripts/get_tilt_angle.py</code> <pre><code>def get_vector(\n    coord1: Tuple[float, float, float], coord2: Tuple[float, float, float]\n) -&gt; Tuple[float, float, float]:\n    \"\"\"\n    Calculate the vector between two coordinates.\n\n    Args:\n        coord1 (Tuple[float, float, float]): First coordinate.\n        coord2 (Tuple[float, float, float]): Second coordinate.\n\n    Returns:\n        Tuple[float, float, float]: Vector from coord2 to coord1.\n    \"\"\"\n    return (coord1[0] - coord2[0], coord1[1] - coord2[1], coord1[2] - coord2[2])\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_tilt_angle.html#pepNmemb.scripts.get_tilt_angle.main","title":"main","text":"<pre><code>main()\n</code></pre> Source code in <code>pepNmemb/scripts/get_tilt_angle.py</code> <pre><code>def main():\n    args_dict = {}\n\n    for k in [\n        \"peptide_name\",\n        \"aminoacid_count\",\n        \"peptide_number\",\n        \"membrane_lipids\",\n        \"step_size\",\n        \"xtc_file_path\",\n        \"tpr_file_path\",\n    ]:\n        args_dict[k] = None\n\n    parser = argparse.ArgumentParser(\n        description=\"Process a file with key-value pairs or command line arguments.\"\n    )\n\n    # Add file argument (optional)\n    parser.add_argument(\"-f\", \"--file\", help=\"Path to input file with key=value pairs\")\n\n    parser.add_argument(\"-xtc\", \"--xtc\", type=str, help=\"Input xtc file path\")\n    parser.add_argument(\"-tpr\", \"--tpr\", type=str, help=\"Input xtpr file path\")\n    parser.add_argument(\"-pname\", \"--pep_name\", type=str, help=\"Peptide name used for saving data\")\n    parser.add_argument(\"-pnum\", \"--pep_num\", type=int, help=\"Number of peptides\")\n    parser.add_argument(\"-res\", \"--res_num\", type=int, help=\"Number of residues in each peptide\")\n    parser.add_argument(\"-mlipids\", \"--memb_lipids\", type=int, help=\"Number of membrane lipids\")\n    parser.add_argument(\"-ss\", \"--step_size\", type=int, help=\"Step size\")\n    parser.add_argument(\"-o\", \"--output_folder\", type=int, help=\"Output folder\")\n\n    args = parser.parse_args()\n\n    if args.file:\n        file_dict = process_file(args.file)\n        args_dict.update(file_dict)\n        args_dict[\"step_size\"] = int(args_dict[\"step_size\"])\n        args_dict[\"aminoacid_count\"] = int(args_dict[\"aminoacid_count\"])\n        args_dict[\"membrane_lipids\"] = int(args_dict[\"membrane_lipids\"])\n        args_dict[\"peptide_number\"] = int(args_dict[\"peptide_number\"])\n    else:\n        args_dict[\"peptide_name\"] = args.pep_name\n        args_dict[\"xtc_file_path\"] = args.xtc\n        args_dict[\"tpr_file_path\"] = args.tpr\n        args_dict[\"peptide_number\"] = args.pep_num\n        args_dict[\"aminoacid_count\"] = args.res_num\n        args_dict[\"membrane_lipids\"] = args.memb_lipids\n        args_dict[\"step_size\"] = args.step_size\n        args_dict[\"output_folder\"] = args.output_folder\n    print(args_dict)\n    try:\n        validate_dict_values(args_dict)\n        print(\"All values validated successfully.\")\n    except NoneValueError as e:\n        print(f\"Error: {e}\")\n        sys.exit(1)\n\n    peptide = Peptide(\n        args_dict[\"peptide_name\"],\n        args_dict[\"xtc_file_path\"],\n        args_dict[\"tpr_file_path\"],\n        args_dict[\"peptide_number\"],\n        args_dict[\"aminoacid_count\"],\n        args_dict[\"step_size\"],\n    )\n\n    print(f\"Starting analysis for peptide {args.pep_name} \" f\"found at {args.xtc}, {args.tpr}\")\n\n    relative_tilt(peptide, args_dict[\"membrane_lipids\"], args_dict[\"output_folder\"])\n\n    print(\n        f\"Starting analysis for peptide {args_dict['peptide_name']} \\\n            found at {args_dict['xtc_file_path']}, {args_dict['tpr_file_path']}\"\n    )\n\n    print(f\"File saved at {args_dict['output_folder']}\")\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_tilt_angle.html#pepNmemb.scripts.get_tilt_angle.relative_tilt","title":"relative_tilt","text":"<pre><code>relative_tilt(obj: Peptide, membrane_lipids: int, output_folder: str) -&gt; DataFrame\n</code></pre> <p>Compute relative tilt angle for the upper bilayer.</p> <p>Args:     obj (Peptide): Peptide object containing trajectory and properties.     membrane_lipids (int): Number of membrane lipids.</p> <p>Returns:     pd.DataFrame: DataFrame with tilt angle information.</p> Source code in <code>pepNmemb/scripts/get_tilt_angle.py</code> <pre><code>def relative_tilt(obj: Peptide, membrane_lipids: int, output_folder: str) -&gt; pd.DataFrame:\n    \"\"\"\n    Compute relative tilt angle for the upper bilayer.\n\n    Args:\n        obj (Peptide): Peptide object containing trajectory and properties.\n        membrane_lipids (int): Number of membrane lipids.\n\n    Returns:\n        pd.DataFrame: DataFrame with tilt angle information.\n    \"\"\"\n    u = obj.u\n    peptide_name = obj.pep_name\n    pep_num_dict = obj.pep_dict\n    frames, n_frames = obj.load_traj()\n    angle_list = []\n\n    for _, _ in tqdm(enumerate(u.trajectory[frames]), total=n_frames):\n        timecount1 = u.trajectory.time\n        if timecount1 != 0:\n            memb_vector = (0, 0, 1)\n\n            p_memb = u.select_atoms(\"resname POPG and name P\").positions[:, [2]].astype(float)\n            p_up = np.mean(p_memb[: int(membrane_lipids / 2)])\n\n            for pep, residues in pep_num_dict.items():\n                pep_coords, _ = get_coords_spanning_memb(u, residues, p_up)\n\n                if len(pep_coords) == 0 or len(pep_coords) &gt; 1:\n                    angle_list.append((timecount1, pep, None, len(pep_coords)))\n                    continue\n\n                points = Points(pep_coords[0])\n                line_fit = Line.best_fit(points)\n                pep_vector = line_fit.vector\n                pep_vector = (pep_vector[0], pep_vector[1], pep_vector[2])\n\n                tilt_angle = angle_between_vectors(memb_vector, pep_vector)\n                angle_list.append((timecount1, pep, abs(90 - tilt_angle), len(pep_coords)))\n\n    df = pd.DataFrame(angle_list, columns=[\"Time (ns)\", \"Peptide_num\", \"Angle\", \"len coords\"])\n    df[\"Angle\"] = df[\"Angle\"].astype(float)\n    df[\"Time (ns)\"] = (df[\"Time (ns)\"].astype(float) / 1000).astype(int)\n\n    df.to_csv(f\"{output_folder}/tilt_angle_{peptide_name}.csv\")\n    return df\n</code></pre>"},{"location":"reference/pepNmemb/scripts/get_tilt_angle.html#pepNmemb.scripts.get_tilt_angle.vector_length","title":"vector_length","text":"<pre><code>vector_length(v: Tuple[float, float, float]) -&gt; float\n</code></pre> <p>Calculate the length of a vector.</p> <p>Args:     v (Tuple[float, float, float]): Input vector.</p> <p>Returns:     float: Length of the vector.</p> Source code in <code>pepNmemb/scripts/get_tilt_angle.py</code> <pre><code>def vector_length(v: Tuple[float, float, float]) -&gt; float:\n    \"\"\"\n    Calculate the length of a vector.\n\n    Args:\n        v (Tuple[float, float, float]): Input vector.\n\n    Returns:\n        float: Length of the vector.\n    \"\"\"\n    return m.sqrt(sum((c**2) for c in v))\n</code></pre>"},{"location":"reference/pepNmemb/scripts/utils.html","title":"utils","text":""},{"location":"reference/pepNmemb/scripts/utils.html#pepNmemb.scripts.utils.NoneValueError","title":"NoneValueError","text":"<pre><code>NoneValueError(key: str, message: Optional[str] = None)\n</code></pre> <p>               Bases: <code>Exception</code></p> <p>Exception raised when a required value is None.</p> Source code in <code>pepNmemb/scripts/utils.py</code> <pre><code>def __init__(self, key: str, message: Optional[str] = None):\n    \"\"\"\n    Init error with message\n    \"\"\"\n    self.key = key\n    self.message = message or f\"Value for key '{key}' cannot be None\"\n    super().__init__(self.message)\n</code></pre>"},{"location":"reference/pepNmemb/scripts/utils.html#pepNmemb.scripts.utils.NoneValueError.key","title":"key  <code>instance-attribute</code>","text":"<pre><code>key = key\n</code></pre>"},{"location":"reference/pepNmemb/scripts/utils.html#pepNmemb.scripts.utils.NoneValueError.message","title":"message  <code>instance-attribute</code>","text":"<pre><code>message = message or f'Value for key '{key}' cannot be None'\n</code></pre>"},{"location":"reference/pepNmemb/scripts/utils.html#pepNmemb.scripts.utils.process_file","title":"process_file","text":"<pre><code>process_file(filename: str) -&gt; Dict[str, str]\n</code></pre> <p>Process the file provided as argument. Extracts key-value pairs in the format key=value where each pair is on a separate line. Returns a dictionary of the extracted key-value pairs.</p> Source code in <code>pepNmemb/scripts/utils.py</code> <pre><code>def process_file(filename: str) -&gt; Dict[str, str]:\n    \"\"\"\n    Process the file provided as argument.\n    Extracts key-value pairs in the format key=value where each pair is on a separate line.\n    Returns a dictionary of the extracted key-value pairs.\n    \"\"\"\n    key_value_dict = {}\n    try:\n        with open(filename) as file:\n            for line_number, line in enumerate(file, 1):\n                line = line.strip()\n                if not line or line.startswith(\"#\"):  # Skip empty lines and comments\n                    continue\n\n                if \"=\" in line:\n                    key, value = line.split(\"=\", 1)  # Split on the first '=' only\n                    key = key.strip()\n                    value = value.strip()\n                    if value == \"\":\n                        value = None\n\n                    if key in key_value_dict:\n                        print(\n                            f\"Warning: Duplicate key '{key}' found on line {line_number}, \\\n                                value will be overwritten\"\n                        )\n\n                    key_value_dict[key] = value\n                else:\n                    print(\n                        f\"Warning: Line {line_number} does not contain a \\\n                              key-value pair in the format 'key=value', skipping: '{line}'\"\n                    )\n\n            print(\n                f\"Successfully processed file '{filename}'. \\\n                    Found {len(key_value_dict)} key-value pairs.\"\n            )\n            return key_value_dict\n\n    except FileNotFoundError:\n        print(f\"Error: File '{filename}' not found.\")\n        return {}\n    except Exception as e:\n        print(f\"Error processing file: {e}\")\n        return {}\n</code></pre>"},{"location":"reference/pepNmemb/scripts/utils.html#pepNmemb.scripts.utils.validate_dict_values","title":"validate_dict_values","text":"<pre><code>validate_dict_values(data: Dict[str, Any], raise_error: bool = True) -&gt; Dict[str, str]\n</code></pre> <p>Validate that no values in the dictionary are None.</p> <p>Args:     data: Dictionary to validate     raise_error: Whether to raise an error (True) or just return invalid keys (False)</p> <p>Returns:     Dictionary containing only invalid keys and their values</p> <p>Raises:     NoneValueError: If any value is None and raise_error is True</p> Source code in <code>pepNmemb/scripts/utils.py</code> <pre><code>def validate_dict_values(data: Dict[str, Any], raise_error: bool = True) -&gt; Dict[str, str]:\n    \"\"\"\n    Validate that no values in the dictionary are None.\n\n    Args:\n        data: Dictionary to validate\n        raise_error: Whether to raise an error (True) or just return invalid keys (False)\n\n    Returns:\n        Dictionary containing only invalid keys and their values\n\n    Raises:\n        NoneValueError: If any value is None and raise_error is True\n    \"\"\"\n    none_values = {k: v for k, v in data.items() if v is None}\n\n    if none_values and raise_error:\n        # Raise error for the first None value found\n        first_key = next(iter(none_values))\n        raise NoneValueError(first_key)\n\n    return none_values\n</code></pre>"}]}